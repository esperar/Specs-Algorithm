package backjoon_kotlin.b_7562

private lateinit var chess: Array<IntArray>
private lateinit var visited: Array<BooleanArray>

fun main() = with(System.`in`.bufferedReader()){
    val t = readLine().toInt()

    while(t < 3){

    }
}